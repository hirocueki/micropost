%h1 ユーザー一覧
- if @users.any?
  %ol.list-group
  - @users.each do | user|
    %li.list-group-item
      = link_to user.email, user
      - if current_user == user
        %span.badge.badge-primary ME!

